# URL of the backend API server, as seen from the browser
# Local dev:
VITE_API_URL=http://localhost:3001
# Server deployment (if API is on the same host at /api via reverse proxy or different port):
# VITE_API_URL=http://your-server.example.com:3001
